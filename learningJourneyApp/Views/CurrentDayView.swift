import SwiftUI

struct CurrentDayView: View {
    
    var body: some View {
        
        VStack{
            Text("Cograts you have reached the currentDayView!")
        }.navigationBarHidden(true)
       
        
    }
}

#Preview {
    CurrentDayView()
}
